overrides:
  install:
    extra_system_packages:
      rpm:
        - fio
      deb:
        - fio
tasks:
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_nfs.TestNFS.test_nfs_export_apply_without_access_type
