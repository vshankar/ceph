overrides:
  ceph:
    log-ignorelist:
      - Replacing daemon mds
tasks:
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_fragment.TestFragmentation.test_dir_merge_with_snap_items
